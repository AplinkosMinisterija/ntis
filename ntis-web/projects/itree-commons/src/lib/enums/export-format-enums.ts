export enum ExportFormatTypes {
  csv = 'CSV',
  pdf = 'PDF',
  excel = 'EXCEL',
  xls = 'XLS',
  xlsx = 'XLSX',
  xml = 'XML',
}

export enum ExportThemes {
  grid = 'grid',
  striped = 'striped',
  plain = 'plain',
}
