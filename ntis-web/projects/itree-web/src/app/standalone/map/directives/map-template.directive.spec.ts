import { MapTemplateDirective } from './map-template.directive';

describe('MapTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new MapTemplateDirective(null);
    expect(directive).toBeTruthy();
  });
});
