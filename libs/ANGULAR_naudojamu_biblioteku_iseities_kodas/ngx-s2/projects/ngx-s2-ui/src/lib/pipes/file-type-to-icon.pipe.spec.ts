import { FileTypeToIconPipe } from './file-type-to-icon.pipe';

describe('FileTypeToIconPipe', () => {
  it('create an instance', () => {
    const pipe = new FileTypeToIconPipe();
    expect(pipe).toBeTruthy();
  });
});
